<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

    <Preference android:title="@string/pref_title_delete_notes"
        android:summary="WARNING: This option will delete all of your notes."
        android:key="clear_notes">

    </Preference>

    <SwitchPreference android:title="@string/pref_night_mode"
        android:summaryOff="Night mode is currently turned off."
        android:summaryOn="Night mode is currently turned on."
        android:key="night_mode">

    </SwitchPreference>
</PreferenceScreen>
